/*async function openNewTabSel() {
    let settings = await browser.storage.sync.get(["replaceNewTab"]);
    let tabInfo = await browser.tabs.getCurrent();
    let tabID = tabInfo.id;
    browser.tabs.create();
}

document.addEventListener("DOMContentLoaded", openNewTabSel());*/